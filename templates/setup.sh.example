#!/usr/bin/env bash
# .claude-team/setup.sh — whatever this repo's AUTHORS need installed before a run, beyond the
# node/browser workflow inputs. Must be executable, or it is skipped.
set -euo pipefail
echo "nothing extra"
